#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: this the character to be checked
 * Return: the value of  1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
