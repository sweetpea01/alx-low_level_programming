#include <stdio.h>
/**
* main - This is the entry point for the program 
*  Return: 0 This always means (Success)
*/
int main(void)
{
int a, c;
 for (a = 0; a < 9; a++)
   {
     for (c = a +1; c < 10; c++)
       {
	 putchar(a + '0');
	 putchar(c + '0');
	 if (a != 8 || c != 9)
	   {
	     putchar(',');
	     putchar(' ');
	   }
       }
   }
 putchar('\n');
 return (0);
}
