#Include <stdio.h>
/**
* main - This is the entry point for our program
* Return: 0 This always means (Success)
*/
int main(void)
{
int c;
for (c = 0; c < 10; c++)
{
putchar(c + '0');
if (c != 9)
{
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
